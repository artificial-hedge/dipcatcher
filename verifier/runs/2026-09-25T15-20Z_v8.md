# Verifier run 2026-09-25T15-20Z_v8 (repo-side gate depth)

- mypy ratchet inverted: `warn_return_any` is now the GLOBAL default for
  fx1 + quant_fund (new code strict by default); the measured 66-module
  debt list carries explicit exemptions (shrink-only). fx1's one Any leak
  (`get_backend` factory) fixed with a cast. 379 + 48 files clean.
- Docs drift guard (`tests/fx1/test_docs_drift.py`): every fx1 command,
  make target, and import path referenced in README + docs/FX1*.md must
  resolve against the real CLI tree / Makefile / package. 21 checks.
- Coverage floor ratcheted 70 → 80: measured 84.93% local macOS, 81.51% CI
  Linux; headroom documented in pyproject. Full canonical suite with
  coverage: 5558 passed, 6 skipped, 1 xfailed in 17m18s.
- Concurrent-agent merge absorbed cleanly (their honesty-inheritance CI
  gate + corpus smoke complement, no duplication).

Verdict: PASS against verifier/v8/acceptance.md, extended.
