# Verifier run 2026-09-25T14-30Z_v8 (Dip bench first real-data receipt)

- New `fx1.bench.run` + `fx1 dipbench --data-dir`: the flagship Dip Quality
  Score bench now runs over real historical bars and emits a receipt with
  per-input SHA-256 provenance, honesty-gated metric keys, and the
  research/backtest disclaimer.
- First real-data receipt: `receipts/dip_bench_crypto_1d_20260925.json` —
  11 crypto 1d series (2023-12-27 → 2026-09-20), 176 dip events at 10%
  threshold. Unconditional recovery: 21.6% (1m), 51.1% (3m), 55.1% (6m),
  74.4% (12m). Climatology forecaster scored in-sample and labeled
  descriptive. Research-scoped, no live claim; the corpus loader accepts it
  as positive evidence (class: research).
- Tests: `tests/fx1/test_bench_run.py` (receipt contract, baseline
  arithmetic, fail-closed empty dir, CLI real-data path).
- Gate: 173 passed / 1 skipped; ruff check+format clean; mypy 48 files.

Verdict: PASS against verifier/v8/acceptance.md, extended.
