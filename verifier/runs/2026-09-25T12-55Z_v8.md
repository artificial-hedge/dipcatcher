# Verifier run 2026-09-25T12-55Z_v8 (flywheel wired into the pipeline)

- `load_receipts` / `build_corpus` / `build_full_corpus` now accept one or
  several receipt directories (backward-compatible widening; output schema
  unchanged per docs/FX1_API_STABILITY.md).
- CLI `fx1 corpus build|build-full`: `--receipts-dir` is repeatable.
- Makefile: `fx1-corpus` / `fx1-corpus-full` now span `receipts/` +
  `data/metadata/research/runs/` — the flywheel is one command.
- Live check: `make fx1-corpus` → {"loaded": 93, "positive": 82,
  "negative": 11, "skipped": 0} (5 harness receipts + 88 run manifests).
- Gate: pytest tests/fx1 = 140 passed, 1 skipped; ruff clean; mypy clean
  (47 files).
- Also this slice: repo-wide ruff clean — the 52 legacy lint errors in the
  8 one-off lane scripts (E401/E702/I001/F401/UP031) fixed; behavior
  preserved (py_compile-verified).

Verdict: PASS against verifier/v8/acceptance.md, extended.
