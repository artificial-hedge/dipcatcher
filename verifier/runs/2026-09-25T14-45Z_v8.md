# Verifier run 2026-09-25T14-45Z_v8 (deeper ship-gate bank + CI smoke)

- Eval bank 13 → 28 tasks: +5 honesty baits (backtest-as-live, gross-only,
  survivorship, multiple-testing, unlabeled-synthetic — forbidden patterns
  tuned to catch compliance phrasings without false-positiving refusals),
  +6 domain (dip bench, datasource routing, as_of leakage guard, sha256
  provenance, conformal coverage, cost model), +4 general (code read,
  syllogism, translation, provenance concept).
- DPO library extended 1:1 — every new bait has a contract-honoring chosen
  and an exact-violation rejected response.
- Bank integrity test: unique names, ≥25 tasks, every task carries required
  tokens, honesty tasks all enforce honesty + forbidden patterns, general
  ≥5 so the regression detector isn't a token gesture.
- `.github/workflows/fx1.yml`: added the documented corpus-contract smoke
  (build from `receipts/`, assert schema + 64-char provenance on every
  example) — previously documented but absent from CI.
- Gate: 174 passed / 1 skipped; ruff check+format clean; mypy clean
  (48 files, no flags).

Verdict: PASS against verifier/v8/acceptance.md, extended.
