# Verifier run 2026-09-25T12-43Z_v8

- pytest tests/fx1: 139 passed, 1 skipped (intentional host-gated skip),
  incl. 3 new corpus tests (manifest eligibility, synthetic labeling,
  explicit-live-claim override) — on a host with no plugin root and no
  agent-gw credentials.
- ruff check src/fx1 tests/fx1: clean.
- mypy src/fx1 --ignore-missing-imports: no issues in 47 source files.
- Flywheel live check: `fx1 corpus build --receipts-dir
  data/metadata/research/runs --out data/fx1/corpus_runs.jsonl` →
  {"loaded": 88, "positive": 77, "negative": 11, "skipped": 0}; every
  positive example carries `receipt_sha256` provenance and an explicit
  SYNTHETIC (simulated-data) label.
- `uv.lock` churn from local `uv run` (marker normalization) reverted —
  not part of this change.
- Derived corpora (`data/fx1/*.jsonl`) are left uncommitted: rebuildable
  via the Makefile fx1-corpus* targets / `fx1 corpus build`.

Verdict: PASS against verifier/v8/acceptance.md (v1–v7 re-verified by the
same gate).
