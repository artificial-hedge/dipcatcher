# Verifier run 2026-09-25T12-43Z_v7 (host-independence fix)

Context: first run on a host *without* the author's plugin root
(`/app/.agents/plugins`) and without agent-gw credentials. v7 as merged
was host-dependent: 11 grammar/routing tests in `tests/fx1/test_sources.py`
failed closed on NO_SCRIPT before reaching the injected runners, and
hypothesis found a real falsifying example in
`test_dip_events_are_causal_and_bounded`.

Fixes (test-side only; production fail-closed semantics unchanged):

- `tests/fx1/test_sources.py`: autouse fixture materializes each non-MCP
  spec's first script candidate under a tmp `FX1_PLUGIN_ROOTS` and sets
  dummy credential env *names* (values are `test-dummy`, never asserted).
  Tests that set their own `FX1_PLUGIN_ROOTS` or delete credentials still
  override it (probe/no-script/no-credentials paths re-verified).
- `tests/fx1/test_properties.py`: dip-event dates were `2026-01-(i%28+1)`,
  repeating after 28 bars; `dates.index()` then returned the first
  occurrence, making "trough ≥ peak" ambiguous. Dates now unique and
  monotonic (`2026-(i//28+1)-(i%28+1)`). Detection code was correct;
  the test's date labels were not.

Gate after fix (this host):

- pytest tests/fx1: 136 passed, 1 skipped (the intentional
  `test_registry_scripts_resolve_on_this_host` skip — plugin root absent)
- ruff check src/fx1 tests/fx1: clean
- mypy src/fx1 --ignore-missing-imports: no issues in 47 source files

Verdict: PASS against verifier/v7/acceptance.md, now host-independent.
