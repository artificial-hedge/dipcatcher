# Verifier run 2026-09-25T14-16Z_v8 (coverage hardening + repo gates)

- New `tests/fx1/test_cli.py` coverage: 14 CLI tests (corpus build/build-full
  multi-dir, dipbench SYNTHETIC label, dpo, curriculum, maskedEval,
  contamination-audit, modelcard ship gate, sbom from real uv.lock,
  sign/attestation incl. fail-closed without key, mrm dossier, eval without
  hosted key fails closed, sources scenarios offline is honest).
  `src/fx1/cli.py` coverage 46% → 83%.
- New `tests/fx1/test_hardening.py`: adapter describe/fetch grammars
  (agent-gw/xhcj/finance-fetch incl. bool flags + non-JSON passthrough),
  runner failure modes (missing binary 127, real timeout 124, truncation
  marker), contamination probes (n-gram flag/no-flag, Min-K% inert/valued,
  rephrased-gap budget, hash-bound report), attestation ladder (quote
  binding/nonce/signature fail-closed, zkML coverage-without-proof rejected,
  tier status on disk artifacts). Three test-authoring bugs caught by the
  suite itself (argv index, truncation locus, gap arithmetic) — fixed in the
  tests; library code needed no changes.
- Gate: pytest tests/fx1 = 170 passed, 1 skipped; ruff check + format clean;
  mypy clean (47 files). fx1 package coverage TOTAL 92%.
- Canonical repo suite (testpaths): green incl. torch-dependent policy-
  gradient tests after `make sync` (now `--all-extras`).
- Repo-wide: `ruff check src scripts tests` clean; dead tests/tests mirror
  (593 files) removed; pyproject comment updated.

Verdict: PASS against verifier/v8/acceptance.md, extended.
