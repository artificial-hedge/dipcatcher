# Dependency hygiene audit — 2026-09-24

## Lock integrity
`uv lock --check` — resolved 173 packages, lockfile consistent with pyproject.

## CVE scan
`pip-audit --strict` over the full export (`--all-extras --all-groups`, 483 locked deps):

| Result | Count |
|---|---|
| Known vulnerabilities | **0** |
| Packages with advisories | 0 |

Consistent with the PROOF.md security row (0 CVEs). No bump actions needed.

## Unused-dependency candidates

| Declared dep | Kind | Evidence | Verdict |
|---|---|---|---|
| `pydantic-settings>=2.6` | **main dep** | No `pydantic_settings`/`BaseSettings` import anywhere in `src/`, `scripts/`, `tests/`; not referenced in docs/Dockerfile; `uv.lock` shows it only via the project's own `>=2.6` specifier | **Genuinely unused** — safe to remove from `project.dependencies` (import-level dead; keep only if a planned BaseSettings migration exists). Confidence: high |
| `pre-commit>=4.0` | dev dep | Not imported; provides the `pre-commit` CLI used by `.pre-commit-config.yaml` hooks | False positive — structural (CLI tool) |
| `pytest-cov>=6.0` | dev dep | Not imported; pytest plugin activated by `--cov` in `Makefile:12` + `ci.yml:130` | False positive — structural (pytest plugin) |
| `types-pyyaml>=6.0` | dev dep | Not imported; mypy stubs for `yaml` | False positive — structural (type stubs) |

## Notes
- All 26 main deps, the `nn` optional group, and the 8 dev-group deps were checked (35 unique names).
- One main dep flagged (`pydantic-settings`); no action taken — report only per task scope.
- Recommendation if removed: drop the specifier from `pyproject.toml` and `uv lock`; verify no transitive resolver pin relies on it (none does — it resolves solely via the project's own specifier).
