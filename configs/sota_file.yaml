# Public file tape (Stooq session-close PIT). Not SIP vintages. Not a live claim.
inherit: base.yaml
runtime:
  mode: research
data:
  source: parquet
  parquet_path: data/file_us/raw
  root: data/file_us
  benchmark_id: SPY
train:
  ranking_target: future_idio_return_1
  ridge_alpha: 0.3
universe:
  min_price: 5.0
  min_adv: 1000000.0
  min_history_bars: 60
  top_n_adv: 50
  exchanges: ["XNYS", "XNAS"]
validation:
  scheme: expanding
  train_bars: 252
  val_bars: 63
  test_bars: 63
  embargo_bars: 5
robinhood_plus:
  enabled: true
  lookback: 32
  pred_len: 5
  sample_count: 4
  variant: mini
  blend_weight: 0.0
  allow_network: false
fusion:
  skip_intervals: true
