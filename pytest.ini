[pytest]
# Exclude the tracked compatibility mirror under tests/tests from canonical discovery.
testpaths =
    tests/unit
    tests/property
    tests/regression
    tests/end_to_end
addopts = -q --strict-markers
markers =
    network: tests that require internet (disabled by default)
    slow: longer integration tests
    synthetic: uses synthetic market data
