{
  "name": "Dipcatcher",
  "user": "ubuntu",
  "install": "bash .cursor/install.sh",
  "terminals": [
    {
      "name": "api",
      "command": "MLFLOW_DISABLE_AGENT_HINT=1 uv run dipcatcher api --host 127.0.0.1 --port 8000",
      "description": "Dipcatcher FastAPI research service on http://127.0.0.1:8000 (loopback; /health, /doctor, /research/latest)."
    }
  ],
  "ports": [
    {
      "name": "api",
      "port": 8000
    }
  ]
}
